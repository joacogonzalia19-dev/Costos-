{
  "name": "costos-",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Calculadora de costos y precios de venta, con integración opcional a Tienda Nube.",
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "node --watch server/index.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
